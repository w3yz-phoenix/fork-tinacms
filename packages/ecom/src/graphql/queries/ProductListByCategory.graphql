query ProductListByCategory($slug: String!) {
  category(slug: $slug) {
    slug
    name
    description
    seoDescription
    seoTitle
    products(first: 100, channel: "tr") {
      edges {
        node {
          id
          ...ProductListItem
        }
      }
    }
  }
}

query ProductList($first: Int!, $after: String) {
  products(first: $first, channel: "tr", after: $after) {
    totalCount
    edges {
      node {
        id
        ...ProductListItem
      }
      cursor
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
}

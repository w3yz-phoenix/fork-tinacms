mutation CheckoutDeleteLines($checkoutId: ID!, $lines: [ID!]!) {
  checkoutLinesDelete(id: $checkoutId, linesIds: $lines) {
    checkout {
      id
    }
    errors {
      field
      code
    }
  }
}
